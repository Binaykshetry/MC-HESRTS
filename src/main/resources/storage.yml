# ==============================================================================
#                               HEARTSS STORAGE CONFIGURATION
# ==============================================================================

# Supported backends: YAML, SQLite, MySQL
backend: "SQLite"

mysql:
  host: "localhost"
  port: 3306
  database: "lifesteal"
  username: "root"
  password: ""
  useSSL: false
